package moduleFn;

// Module-level functions. `_` is a perfectly valid Haxe identifier for a top-level
// function, even though `_` is also used as a wildcard elsewhere.
function _(value:String):String {
  return value;
}

function wrap(value:String):String {
  return value;
}
