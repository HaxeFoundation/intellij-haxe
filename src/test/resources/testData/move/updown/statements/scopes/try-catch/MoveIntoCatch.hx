class TestClass {
    function main() {
        try
        {
        }
        catch (firstBlock)
        {
        }
        var multiLine<caret> =
        [
            "multi line",
            "string",
            "variable"
        ];
    }
}