class TestClass {
    function main() {
        try
        {
            var multiLine =
            [
                "multi line",
                "string",
                "variable"
            ];
        }
        catch (firstBlock)
        {
        }
    }
}