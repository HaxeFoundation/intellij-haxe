class TestClass {
    // NOTE: the strange code format is intentional to verify that we move the correct line ranges
    function main() {
        var multiLine =
        [
            "multi line",
            "string",
            "variable"
        ];
        do
        {
        }
        while
        (true);
    }
}